<div class="card mb-2">
  <div class="card-body">
    <%= link_to 'X', organization_path(organization),
            class: 'float-right btn btn-sm btn-danger',
            method: :delete,
            data: { confirm: 'Are you sure you want to destroy ' + organization.name + '?'} %>
    <h3><%= link_to organization.name, organization_path(organization) %></h3>
    <%= link_to 'Edit', edit_organization_path(organization), class: 'float-right btn btn-sm btn-secondary' %>
  </div>
</div>
